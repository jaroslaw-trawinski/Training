apiVersion: v1
kind: Pod
metadata:
  creationTimestamp: null
  labels:
    app: nginx      
    run: nginx1
  name: nginx1
spec:
  containers:
   - image: nginx
     name: nginx
     ports:
            - containerPort: 80
     resources: {}
     volumeMounts:
            - name: workdir
              mountPath: /usr/share/nginx/html
  initContainers:
    - image: busybox
      name: init
      args:
        - sh
        - -c
        - echo "Hello from instance 1" > /volume/index.html
      volumeMounts:
            - name: workdir
              mountPath: /volume
  dnsPolicy: ClusterFirst
  volumes:
          - name: workdir
            emptyDir: {}
